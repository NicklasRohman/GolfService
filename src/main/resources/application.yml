spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:8080/golfprogram
    username: root
    password:
  banner:
    location: banner.txt
  liquibase:
    change-log: sql/changelog.xml
    enabled: true
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: none
  application:
    name: golf service

server:
  port: 8080
